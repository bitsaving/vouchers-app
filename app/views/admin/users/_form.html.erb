<div class="align">
   <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>
      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>

      </ul>
    </div>
  <% end %>   
  <%= form_for [:admin, @user], :html => { :class => 'form-horizontal' } do |f| %>
    <div class="control-group">
      <%= f.label :email, "Email", :class => 'control-label compulsory-field' %>
      <div class="controls">
        <%= f.text_field :email, :class => 'text_field' %>
      </div>
    </div>

    
    <div class="control-group">
      <%= f.label :first_name, "First name",:class => 'compulsory-field control-label' %>
      <div class="controls">
        <%= f.text_field :first_name, :class => 'text_field' %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :last_name, "Last name", :class => 'compulsory-field control-label' %>
      <div class="controls">
        <%= f.text_field :last_name, :class => 'text_field' %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :admin, :class => 'control-label' %>
      <div class="controls">
        <%= f.check_box :user_type  %><br />
      </div>
   </div>
    <div>
      <%=f.hidden_field :user_type ,:class=>"user_type"%>
    </div>
    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary'%>
    </div>
  <%end%>
</div>