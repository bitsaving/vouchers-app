<% @accounts.each do |account| %>
  <tr>
    <td><%= link_to account.name, account_path(account) %></td>
    <td>
     <%= link_to "edit",edit_account_path(account), :class => 'btn btn-mini btn-primary' %></td>
  </tr>
<% end %>
