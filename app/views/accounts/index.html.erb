<% #FIXME_AB: HTML indentation is weired -%>
<% #FIXME_AB: Why we are doing following assignment -%>
  <%- model_class = Account -%>
<div class="page-header">
<h3> Accounts </h3>
</div>
<div class="new_button">
  <% #FIXME_AB: Lets not use translations as we are not using other places. Make this change through out the app -%>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_account_path,
            :class => 'btn new_button btn-primary' %>
</div>
<% #FIXME_AB: @accounts.count > 0 can be written as @accounts.present? for better code readability  -%>
<% if @accounts.count >0 %>
<table class="table table-striped accountss">
  <thead>
    <tr>
      <% #FIXME_AB: looks like following code is generated by scaffold. Since this is not a dynamic code and we are sure that it is a Account model so us it and avoid using extra method calls -%>
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
   <% @accounts.each do |account| %>
      <tr>
        <!--   -->
        <td><%= link_to account.name, account_path(account) %></td>
   
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_account_path(account), :class => 'btn btn-mini btn-primary' %>
         
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
 <div id="paginator">
 
 <%=paginate @accounts, :remote => true %>
  </div>
<%end%>