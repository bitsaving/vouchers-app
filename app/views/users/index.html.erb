<%- model_class = User -%>
<div class="page-header">
  
</div>
<div class ="disable_input">
<%= render :partial => 'form' %>
</div>
 <%= link_to t('.back', :default => t("helpers.links.back")),
                      new_user_session_path, :class => 'btn btn-mini' %>