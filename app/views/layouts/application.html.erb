<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "VoucherApp" %></title>
  <%= csrf_meta_tags %>

<!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
<!--[if lt IE 9]>
  <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag 'print', :media => :print %>
  <%= stylesheet_link_tag "application",:media=> :all%>


  <!-- For third-generation iPad with high-resolution Retina display: -->
  <!-- Size should be 144 x 144 pixels -->
  <%= favicon_link_tag 'apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

  <!-- For iPhone with high-resolution Retina display: -->
  <!-- Size should be 114 x 114 pixels -->
  <%= favicon_link_tag 'apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

  <!-- For first- and second-generation iPad: -->
  <!-- Size should be 72 x 72 pixels -->
  <%= favicon_link_tag 'apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

  <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
  <!-- Size should be 57 x 57 pixels -->
  <%= favicon_link_tag 'apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

  <!-- For all other devices -->
  <!-- Size should be 32 x 32 pixels -->
  <%= favicon_link_tag 'vinsol.ico', :rel => 'shortcut icon' %>

  <%= javascript_include_tag "application" %>
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top onprint">
      <div class="navbar-inner">
        <div class="container-fluid color">
          <a class="brand" href="#">VoucherApp</a>


      <% if current_user %> 
            <div class="nav-collapse collapse custom-form">
              <p class="navbar-text pull-right">
                Hi, <%= current_user.name %> &nbsp;
                <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
              </p>
              <form action="<%= search_path %>" method="get" class="pull-right">
              <%= text_field_tag :query, params[:query], placeholder: 'Search vouchers...' %>
              <!-- <%= submit_tag "Search", name: nil, class: 'btn btn-primary' %> -->
              </form>
              <form action="<%= filter_path %>" method="get" class="pull-right">
              <%= select_tag :year, options_for_select(populate_dropdown, :selected => session[:date]) %>
              </form>
            </div><!--/.nav-collapse -->
            <ul class="nav nav-pills pull-right">
              <li><%= nav_link t('.home'), root_path%></li> 
              <li><%= nav_link t(".vouchers") , vouchers_path %></li>
              <% if current_user.admin?%>
              <li><%= nav_link t(".users") , admin_users_path%></li>
              <%end%>
              <li><%= nav_link t(".accounts") , accounts_path%></li>
              <li><%= nav_link t(".report") , report_path %></li>
              <hr>
            </ul>
          <%end%>

        </div>
      </div>
    </div>
    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span12">
          <div class="hero-unit color-hero">
            <%= bootstrap_flash %>
            <%=yield%>
          </div>
        </div><!--/span-->
      </div><!--/row-->
     
      <footer class="onprint">
         <hr>
        <p>© Vinsol 2014</p>
      </footer>
    </div><!--/.fluid-container-->

</body>
</html>
